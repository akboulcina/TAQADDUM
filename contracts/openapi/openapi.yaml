openapi: 3.1.0
info:
  title: TAQADDUM API
  version: 0.1.0
paths:
  /health/live:
    get:
      operationId: healthLive
      responses:
        '200': { description: OK }
  /health/ready:
    get:
      operationId: healthReady
      responses:
        '200': { description: OK }
